sources:
  - name: jaffle_shop
    schema: jaffle_shop_original
    database: raw
    tables:
      - name: customers
      - name: orders
        freshness:
          warn_after:
            count: 2
            period: day
          error_after:
            count: 4
            period: day
        loaded_at_field: _etl_loaded_at
